Just completed the AWS setup of ubuntu in a virtual machine.

So I guess Docker is more used in GCP with K8s and AWS uses its own type of Virtual Machine using Amazon Machine Image (AMI).

This is the type entries I need to publish in GH daily.

Managing your thoughts about CS topics.